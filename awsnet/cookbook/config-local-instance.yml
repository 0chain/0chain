---
- hosts: localhost

  vars_files:
    - "{{ credential }}/aws/credentials.yml"
    - common/keyregistry.yml
    - common/regions.yml
    - common/miner.yml
    - common/sharder.yml
    - common/receipes.yml

  environment: "{{aws_vars}}"

  roles:
    - role: common
    - role: control
    - role: keyadmin
    - role: instances
    - role: storage
    - role: miners
    - role: sharders
...