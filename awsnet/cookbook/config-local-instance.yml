---
- hosts: localhost

  vars_files:
   - "{{ credential }}/aws/credentials.yml"

  environment: "{{aws_vars}}"

  roles:
    - role: common
    - role: keyadmin
    - role: instances
    - role: gitrepo
    - role: storage
    - role: miners
    - role: sharders
...