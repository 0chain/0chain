---
- hosts: localhost

  vars_files:
    - "{{ credential }}/aws/credentials.yml"
    - [ "anchor/{{network}}/blueprint.yml"]
    - [ "anchor/{{network}}/clientid.yml"]

  roles:
    - role: artifacts
    - role: instance
    - role: istart
    - role: istop
    - role: gitrepo
    - role: agent-clientid
    - role: agent-build
    - role: agent-role
...