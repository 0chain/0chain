---
- include_tasks: block-explorer-assemble-local.yml
  tags: ['block-explorer-assemble-local']
  loop: "{{explorers}}"
  loop_control:
    loop_var: agent

- include_tasks: block-explorer-teardown-local.yml
  tags: ['block-explorer-teardown-local']

- include_tasks: block-explorer-assemble-remote.yml
  tags: ['block-explorer-assemble-remote']
...