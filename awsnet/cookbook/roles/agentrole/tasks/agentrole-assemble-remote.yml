---
- set_fact:
    zchain_code: "{{artifact.zchain.code}}"

- name: "Spawn the sharder"
  shell: docker.aws/bin/spawn.sharder.sh
  args:
    chdir: "{{zchain_code}}"

- name: "Spawn the miner"
  shell: docker.aws/bin/spawn.miner.sh
  args:
    chdir: "{{zchain_code}}"
...