---
- block:
  - set_fact:
      order_regions: "{{regions | sort}}"
      AMI: "{{AMI | default({})}}"

  - include_tasks: ami-locator-local.yml
    tags: ['ami-locator-local']
    loop: "{{order_regions}}"
    loop_control:
      loop_var: region
  tags: ['ami-locator-local']
...

