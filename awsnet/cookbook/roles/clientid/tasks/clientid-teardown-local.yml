---
- name: "Remove records from Route53 database"
  route53:
    state: absent
    zone: testnet.0chain.net
    record: "{{item.tags.Name}}.testnet.0chain.net"
    value: "{{item.public_ip_address}}"
    type: A
    wait: yes
    ttl: 60
  with_items: "{{nodes}}"
...