---
- name: "Prepare miner instance"
  set_fact:
    network_type: "{{znetwork.network_type}}"
    network_name: "{{znetwork.network_name}}"

- import_tasks: instance-assemble.yml
  tags: ['instances-assemble-local']

- import_tasks: instance-teardown.yml
  tags: ['instances-teardown-local']

- import_tasks: instance-prime.yml
  tags: ['instances-assemble-remote']
...

