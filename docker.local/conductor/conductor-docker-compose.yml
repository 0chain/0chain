version: '3'
services:
  conductor:
    build:
      context: ./../../
      dockerfile: docker.local/conductor/Dockerfile
    ports:
      - "15211:15211"
    # volumes:
    #   - ../config:/0chain/config
    #   - ../conductor/log:/log
    networks:
      # default:
      testnet0:
        ipv4_address: 198.18.0.152
    command: /bin/conductor -config /etc/conductor.yaml

networks:
  #default:
  #  driver: bridge
  testnet0:
    external: true

# volumes:
#   logs:
#   config:
