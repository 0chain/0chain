name: CONDUCTOR_TEST

on:
  workflow_dispatch:
    inputs:
      view_change: 
        description: 'Type true/false for setting view change.'
        default: 'false'
        required: true

jobs:
  conductor: 
    name: CONDUCTOR-TEST
    runs-on: self-hosted
    
    steps:
    - name: Git Checkout
      uses: actions/checkout@v2

    - name: Get Branch
      run: |
        echo "Tag ${TAG}."
        BRANCH=$(echo ${GITHUB_REF#refs/heads/} | sed 's/\//-/g')
        SHORT_SHA=$(echo $GITHUB_SHA | head -c 8)
        echo "Branch ${BRANCH}."
      env:
        TAG: ${{ github.event.inputs.view_change }}

    - name: Completing.
      run: |
        ls -lha
        
    - name: Build Miner Docker Image For Integration Test.
      run: |
        cd /root/actions-runner/chain/0chain/0chain
    