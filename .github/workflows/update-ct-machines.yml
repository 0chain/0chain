name: Update CT machines dependencies

on:
  workflow_dispatch:

jobs:
  update-ct-machines:
    strategy:
      matrix:
        machine: ["conductor-test02", "conductor-test03", "conductor-test04", "conductor-test05", "conductor-test06", "conductor-test07", "conductor-test08", "conductor-test09", "conductor-test10", "conductor-test11", "conductor-test12"]
    uses: 0chain/0chain/.github/workflows/install-conductor-testing-dependencies-on-ubuntu-server.yaml@wf/fix/update-all-ct-machines
    with:
      runner-label: ${{ matrix.machine }}
